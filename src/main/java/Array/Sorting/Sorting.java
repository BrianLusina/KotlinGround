package Array.Sorting;

import java.util.Arrays;
import java.util.stream.IntStream;
import java.util.stream.Stream;

class Sorting {
    /**
     * Loop through each int in the array, looking for 3 and 7, store their positions in variables
     * swap their positions and return the result*/
    static Integer[] sortTwisted37(Integer[] array) {
        //Stream.stream(array).forEach();

        return array;
    }

}
