package java.math.NumPow;

import java.util.ArrayList;
import java.util.stream.IntStream;

public class PowerSumDig {
    /**
     * Get each digit of the number and sum it
     * Converts the number to a string, determines length and raises each digit to the power of that length
     * sums the output
     * <p>
     * Check if both are equal, add to a list
     */
    public static long powerSumDigTerm(int n) {
        int output = 0;
        ArrayList<Integer> nums = new ArrayList<>();
        for (Integer number : IntStream.range(1, 10).toArray()) {

        }
        return 5;
    }
}
