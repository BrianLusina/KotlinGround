machine:
  java:
    version: openjdk 7

dependencies:
  pre:
    - gradle dependencies

test:
  override:
    - gradle test
    # runs coverage
    # - coverage run --source pysnips -m py.test tests/
    # - coverage report
    # - coverage xml -i
    # - coverage html -i
    # - python-codacy-coverage -r coverage.xml
    # - codecov
